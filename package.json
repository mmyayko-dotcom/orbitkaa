{
  "name": "orbita-app",
  "version": "1.0.0",
  "private": true,
  "description": "Orbita — счётчик дней",
  "scripts": {
    "build": "echo 'static site, no build step needed'"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "@capacitor/assets": "^3.0.5"
  }
}
